import Footer from "@/components/Footer";
import { DefaultLayout } from "@/layouts/default.layout";

const Home = () => {
  return (
    <DefaultLayout>
      <Footer />
    </DefaultLayout>
  );
};

export default Home;
